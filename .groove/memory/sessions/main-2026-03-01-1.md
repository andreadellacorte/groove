---
name: main-2026-03-01-1
branch: main
started: 2026-03-01T12:16:07Z
status: ended
---

# Session: main-2026-03-01-1

**Started**: 2026-03-01
**Branch**: main
**Goal**: Design the groove user layer — learned memory tier, don't-edit boundaries for core skill files, and compound loop routing for workflow learnings

---

## Work Done

- Shipped v0.8.0: `groove:managed` comments in all SKILL.md files, Constraints section in prime block, `.groove/memory/learned/` directory, workflow learning routing in `work compound` and `memory session end`
- Reworked AGENTS.md architecture across v0.8.1–v0.8.8: `groove prime` now outputs to conversation only; `groove install` owns the AGENTS.md bootstrap directly; verbose `beans prime` output replaced with 2-line stubs
- Fixed migration filter (v0.8.5): filter now uses `To > local version`, not `From >= local version` — migrations apply correctly across any version gap
- Fixed `groove update` bootstrapping gap (v0.8.6): agent re-reads `update.md` from disk after `npx skills add` so updated logic takes effect in the same run
- Moved `last-version-check` to `.groove/.cache/` (v0.8.3): cache data no longer lives in the config file; `.cache/` is always gitignored
- Added migrations to fix existing AGENTS.md files on `groove update` (v0.8.4, v0.8.7, v0.8.8)
- Ran `groove update` locally twice to exercise the update path (v0.8.1, v0.8.3, v0.8.8)
- Captured workflow learning in `.groove/memory/learned/patterns.md`: test migrations through the system, not manually

## Decisions

- `groove prime` is conversation-only; AGENTS.md bootstrap is static, owned by `groove install`
- `groove:task` section is a 2-line stub pointing to `beans prime` on demand — full CLI reference is never embedded in AGENTS.md
- Migration `From` field is informational only; filter gates on `To` version
- `.groove/.cache/` is always gitignored regardless of git strategy; `.gitkeep` is the only tracked file
- Workflow learnings go to `.groove/memory/learned/<topic>.md`; `patterns.md` created this session

## Next Steps

- Test the full migration path on a repo that is at an old version (e.g. 0.6.x) to confirm all 0.8.x migrations run correctly end-to-end
- Create GitHub releases for v0.8.x tags so `groove check` / `groove prime` version checks reflect the latest shipped version
